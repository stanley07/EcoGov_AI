# WF-2 Database Model

Status: Proposed for independent review

## Conventions

- UUID primary keys; UTC `TIMESTAMPTZ`; database time authoritative.
- Tenant-owned tables have `UNIQUE (tenant_id, id)` and tenant-leading indexes.
- Organization references use `(tenant_id, organization_id)` composite foreign keys.
- Mutable aggregates have positive `version` for compare-and-set updates.
- Published content and status history are append-only/immutable through restricted triggers.
- JSON is schema-versioned, size-bounded, and used only where relational columns cannot express the contract safely.
- Secrets and unrestricted message bodies are stored as encrypted envelopes or secret/object references, never plaintext columns.

## Exact entities

| Entity                                 | Purpose and required columns                                                                                                                                                                                                                                                                                              | Principal constraints                                                                                                                                                                    |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `notification_template`                | Stable semantic template: `id`, nullable `tenant_id`, `ownership_type`, `application_key`, `template_key`, `name`, `description`, `status`, `allow_tenant_override`, `data_classification`, `created_by`, `created_at`, `updated_at`, `version`                                                                           | `ownership_type IN ('platform','application','tenant')`; tenant owner requires tenant; platform/application owner forbids tenant; unique owner namespace/key; status `active/deprecated` |
| `notification_template_version`        | `id`, template identity, `version_number`, `status`, `variable_schema`, `classification`, `validation_report`, `content_hash`, `published_at/by`, `deprecated_at/by`, `created_at/by`, `version`                                                                                                                          | lifecycle `draft -> validating -> published -> deprecated`; unique template/version; published immutable; no deprecated republish                                                        |
| `notification_template_rendering`      | One rendering per version/channel/locale: `subject_template`, encrypted/body reference, `content_type`, `sender_profile_key`, `metadata_schema`, `created_at`                                                                                                                                                             | channel `email/sms/in_app/webhook/push`; locale normalized; unique `(version_id,channel,locale)`; immutable when parent published                                                        |
| `notification_template_binding`        | Tenant-local resolution: `tenant_id`, `id`, `semantic_key`, `application_key`, `template_version_id`, `organization_id`, `is_default`, `status`, `effective_from/to`, `version`                                                                                                                                           | active binding references published version; organization same tenant; one active default per tenant/org/application/key; platform/application catalog use only through binding          |
| `notification_channel_policy`          | Tenant/org channel enablement and constraints: enabled channels, mandatory/emergency rules, quiet-hours policy, rate-limit policy, provider route ID, retention class                                                                                                                                                     | tenant/org-safe; versioned; no secret material                                                                                                                                           |
| `notification_user_preference`         | `tenant_id`, `user_id`, optional `organization_id`, `semantic_key` or category, `channel`, `preference`, `quiet_hours_id`, timestamps/version                                                                                                                                                                             | preference `allow/deny/inherit`; exact user tenant; unique scope/category/channel                                                                                                        |
| `notification_quiet_hours`             | Tenant/org/user schedule: time zone, days, local start/end, emergency behavior, status/version                                                                                                                                                                                                                            | valid IANA zone; non-overlapping windows; DST validation; bounded windows                                                                                                                |
| `notification_suppression`             | Tenant/channel/destination digest or user scope, `reason_code`, `source`, `starts_at`, `expires_at`, `legal_hold`, `created_by`                                                                                                                                                                                           | no raw destination; bounded/indefinite approved reasons; tenant scoped; append-only expiry/revoke history                                                                                |
| `notification_request`                 | Aggregate: `tenant_id`, `organization_id`, `id`, producer/application, semantic/template binding and pinned version, classification, source type/id, workflow instance/event IDs, correlation ID, idempotency key, request hash, priority, mandatory/emergency flags, schedule/expiry, state, counts, version, timestamps | unique `(tenant_id,producer_namespace,idempotency_key)`; hash conflict is 409; pinned published template; organization same tenant                                                       |
| `notification_recipient`               | Immutable resolution snapshot: request, selector type/source ID, user ID, organization ID, locale/time zone, resolution status/reason, created_at                                                                                                                                                                         | selector vocabulary exact; user/org tenant-safe; no raw destination; unique request/recipient identity                                                                                   |
| `notification_destination`             | Protected destination snapshot: recipient, channel, destination digest, encrypted destination/reference, verification state, source, created_at                                                                                                                                                                           | unique recipient/channel/digest; destination never returned unmasked; verification required by channel policy                                                                            |
| `notification_delivery`                | One recipient/channel outcome: request/recipient/destination, state, provider route/version, current provider, deduplication identity/window, priority, scheduled/next-attempt/expiry, attempt/max counts, lease task ID, provider message ID digest, version/timestamps                                                  | unique `(tenant_id,deduplication_identity)`; state constraint; terminal CAS; tenant-safe references                                                                                      |
| `notification_delivery_attempt`        | Append-only attempt: delivery, sequence, provider ID, route position, fencing token, state, classification, provider response code class, latency, started/finished, retry-after, safe failure code, request/response metadata hashes                                                                                     | unique delivery/sequence and delivery/fencing token; no raw provider body or credential                                                                                                  |
| `notification_delivery_status_history` | Ordered append-only status evidence: request, delivery optional, sequence, from/to state, reason, actor type/ID, attempt/task/outbox IDs, correlation ID, timestamp                                                                                                                                                       | unique request/sequence; restricted insert service; update/delete prohibited                                                                                                             |
| `notification_provider`                | Platform provider catalog: key, channel, adapter/version, capabilities, status, region/data-residency metadata                                                                                                                                                                                                            | no credentials; allowlisted adapters only; status `active/draining/disabled`                                                                                                             |
| `notification_provider_route`          | Tenant/org routing policy: channel, ordered provider entries, failover policy, sender profile, status/version/effective period                                                                                                                                                                                            | tenant/org-safe; published immutable version or snapshot hash; compatible channel/provider only                                                                                          |
| `notification_provider_route_entry`    | Route position, provider, secret reference, timeout, per-provider rate policy, allowed classifications                                                                                                                                                                                                                    | unique route/position; secret reference only; active provider required for publish                                                                                                       |
| `notification_webhook_endpoint`        | Tenant/org/application endpoint: URL ciphertext, URL origin digest, ownership status, signing algorithm/key reference/version, allowed event types, status, rotation metadata, version                                                                                                                                    | HTTPS; verified ownership; SSRF allow/deny rules; no raw secret; unique tenant/application/origin identity                                                                               |
| `notification_webhook_challenge`       | One-time ownership challenge digest, endpoint, expiry, attempts, state                                                                                                                                                                                                                                                    | short-lived; digest only; one active challenge; replay-safe                                                                                                                              |
| `notification_rate_limit_bucket`       | Durable tenant/org/channel/provider token bucket or fixed-window counters                                                                                                                                                                                                                                                 | deterministic bucket key; bounded refill/capacity; DB atomic update; no PII dimension                                                                                                    |
| `notification_deduplication_record`    | Tenant-scoped semantic duplicate ledger: identity, request/delivery, payload hash, first/last seen, expires_at, result                                                                                                                                                                                                    | unique tenant/identity; same hash replays; different hash conflicts                                                                                                                      |
| `notification_inbox_item`              | In-app durable projection: tenant/org/user, request/delivery, title/body protected reference, category, priority, state, read/archived timestamps, version                                                                                                                                                                | state `unread/read/archived`; same-tenant active user; unique delivery; user can mutate only own item                                                                                    |
| `notification_provider_callback`       | Idempotent callback receipt: tenant/provider, callback ID/digest, signature key version, received timestamp, timestamp/nonce, payload hash, delivery match, disposition                                                                                                                                                   | unique provider/callback identity and nonce window; protected payload reference; append-only                                                                                             |
| `notification_audit_event`             | Notification-specific append-only audit: tenant/org, action, result, actor, resource IDs, reason, correlation, redacted context, created_at                                                                                                                                                                               | IDs/codes/hashes/counts only; no destination/body/secret                                                                                                                                 |

## Existing entities reused or extended

| Existing entity                                                              | Decision                                                                                                  | WF-2 use                                                                                                                       |
| ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `outbox_event`                                                               | REUSE, then narrowly EXTEND only if independent review requires tenant-scoped dedupe/fencing              | Intake of committed facts and publication of notification lifecycle facts; never the provider delivery ledger                  |
| `task_execution`                                                             | REUSE/EXTEND                                                                                              | Delivery tasks, existing encrypted payload, 60-second lease policy for WF-2, fencing token from migration 33, bounded attempts |
| `idempotency_record`                                                         | REUSE for HTTP command response replay; NEW notification dedupe ledger for cross-producer semantic dedupe | API replay remains actor/operation scoped; delivery dedupe remains tenant/semantic scoped                                      |
| `authz_audit_log`, platform audit, workflow event/audit                      | REUSE                                                                                                     | Authorization decisions and originating workflow history; notification-specific evidence links by IDs                          |
| `tenant`, `organization`, `user_account`, `membership`, `role`, `permission` | REUSE                                                                                                     | Isolation, active-recipient resolution, exact RBAC                                                                             |

## Required indexes

- Claimable delivery: `(state,next_attempt_at,priority,created_at)` partial for `queued/scheduled/transient_failed/rate_limited`.
- Request browse: `(tenant_id,organization_id,created_at DESC,id)`.
- Inbox: `(tenant_id,user_id,state,created_at DESC,id)`.
- Attempts: `(tenant_id,delivery_id,sequence_number)`.
- Status history: `(tenant_id,request_id,sequence_number)`.
- Webhook callback replay: `(provider_id,nonce,timestamp)` plus expiry maintenance.
- Suppression lookup: `(tenant_id,channel,destination_digest,starts_at,expires_at)`.
- Recipient role/org resolution relies on existing tenant-leading membership indexes; migration preflight must prove them.

## Retention and deletion

- Templates, request headers, attempts, state history, and audit follow regulated evidence retention.
- Message content/destination envelopes use shorter classification-specific retention and cryptographic erasure where legally permitted.
- Inbox archival is not audit deletion.
- Legal hold overrides scheduled purge and is itself audited.
- Tenant deletion remains a governed lifecycle; foreign keys use `RESTRICT`, not cascading evidence loss.
